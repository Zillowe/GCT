name: "GCT"
packages:
  - name: "go"
    check: "go version"

commands:
  - cmd: "build"
    run: "./build/build.sh"
  - cmd: "build-r"
    run: "./build/build-release.sh"
  - cmd: "fmt"
    run: "go fmt ./..."
  - cmd: "lint"
    run: "golangci-lint run"
